# 폴더 가이드

My Planner 저장소의 주요 폴더와 역할을 설명합니다.

## `DEV/`

실제 애플리케이션을 개발하는 소스 코드 묶음입니다.

다음과 같은 내용을 이 폴더에서 관리합니다.

- React·TypeScript UI 소스
- Tauri 애플리케이션 소스
- 컴포넌트, 페이지 및 상태 관리 코드
- Domain과 Application Core 구현
- 테스트 코드와 개발 설정
- `package.json` 등 빌드 및 실행에 필요한 파일

설계 아이디어나 업무 프로세스 설명만을 위한 문서는 가급적 이 폴더에 두지 않습니다. 특정 코드와 직접 관련된 기술 문서나 개발 환경 안내는 `DEV/docs/`에서 관리할 수 있습니다.

## `work_concept/`

전체 시스템 설계안과 개발 작업에 필요한 개념을 관리합니다.

다음과 같은 내용을 이 폴더에 정리합니다.

- 제품 및 시스템의 핵심 원칙
- 전체 시스템 아키텍처
- Domain Model과 데이터 구조
- Markdown·SQLite 등 저장소 설계
- Tauri IPC 및 모듈 책임
- Local LLM, 보안, 성능 및 테스트 전략
- 기술 선택의 트레이드오프
- 개발 로드맵과 확장 방향

실제 사용 순서보다 “무엇을 왜 이렇게 설계하는가”를 설명하는 문서를 배치합니다.

이 영역의 시작 문서는 `work_concept/SYSTEM_DESIGN.md`입니다.

## `workflow/`

시스템이 실제로 동작하는 흐름과 사용자가 마주하는 업무 프로세스를 관리합니다.

다음과 같은 내용을 이 폴더에 정리합니다.

- 최초 실행과 Workspace 선택 과정
- 사용자의 일반적인 앱 이용 흐름
- Task·Project·Event 생성, 수정, 삭제 과정
- Priority·Gantt·Calendar 이용 과정
- AI Command 입력, Preview, 확인 및 적용 과정
- 외부 파일 변경과 동기화 과정
- 충돌 처리와 장애 복구 절차
- 사용자 행동에 따른 시스템 내부 처리 순서

시스템 개념보다 “누가 어떤 행동을 하면 시스템이 어떤 순서로 처리하는가”를 설명하는 문서를 배치합니다.

이 영역의 시작 문서는 `workflow/WORKFLOW_OVERVIEW.md`입니다.

## `Constraint/`

제품과 시스템을 설계·구현할 때 반드시 지켜야 하는 제약사항을 관리합니다.

다음과 같은 내용을 이 폴더에 정리합니다.

- 최소 CPU·RAM 등 지원 실행 환경
- Offline-first와 Local-first 조건
- Local LLM의 역할과 허용 범위
- 모델 크기, 메모리, 응답 시간 및 context 제약
- 자연어 명령의 검증, Preview 및 사용자 확인 경계
- 데이터, 개인정보 및 Workspace 접근 제한
- AI 상태와 실패를 표현하기 위한 UI 요구사항
- 모델 후보의 검증 절차와 평가 기준
- MVP에서 지원하지 않는 범위

`work_concept/`의 설계와 `workflow/`의 처리 과정은 이 폴더의 제약사항을 충족해야 합니다. 새로운 제약이나 기존 제약의 변경은 앞으로 `Constraint/`에서 관리합니다.

## `LLM/`

Local LLM을 실제로 실행하고 평가한 방법과 결과를 관리합니다.

다음과 같은 내용을 이 폴더에 정리합니다.

- `llama.cpp` 설치, 빌드 및 실행 방법
- GGUF 모델 다운로드와 보관 방법
- CPU 추론 옵션과 성능 측정 방법
- 일정관리용 Prompt와 구조화 출력 실험
- 모델별 성공·실패 사례와 평가 결과
- 앱 통합 전에 확인한 기술적 결론
- Local LLM 사용 중 발생하는 문제와 해결 방법

`work_concept/`는 LLM의 구조와 역할을, `Constraint/`는 LLM이 지켜야 할 조건을 설명합니다. `LLM/`은 그 설계와 제약을 실제 모델로 어떻게 시험했고 어떤 결과를 얻었는지 기록합니다. GGUF 모델 파일과 native binary는 이 폴더에 커밋하지 않습니다.

`LLM/` 내부는 다음 세 책임으로 분리합니다.

```text
01-llama-cpp-guide/  # llama.cpp 설치와 사용 방법
02-test-runs/        # 실제 모델 테스트 실행 내역
03-test-case-design/ # 테스트 케이스와 판정 방식 설계
```

이 영역의 시작 문서는 `LLM/LOCAL_LLM_OVERVIEW.md`입니다.

## 문서 분류 기준

새 문서를 추가할 때 다음 기준을 사용합니다.

```text
실제로 실행되는 코드인가?
└─ DEV/

시스템의 구조, 원칙 또는 기술적 결정인가?
└─ work_concept/

사용자 또는 시스템의 처리 순서를 설명하는가?
└─ workflow/

설계와 구현이 반드시 지켜야 하는 조건 또는 한계인가?
└─ Constraint/

Local LLM을 실행·평가한 방법이나 결과인가?
└─ LLM/
```

한 문서에 설계 개념, 처리 흐름, 제약사항, LLM 실험 결과가 함께 포함되어 있다면 내용을 각 폴더로 분리하고 서로 링크하는 방식을 권장합니다.
